# 내일(2026-09-26) 작업 목록 · 2026-09-25 세션 인계

## 0. 바로 시작할 것: 내부 테스트 업로드 마무리 (🙋 사용자 먼저)
- **설계 정본**: `docs/BUILD.md` §4 (선행 조건 · 업로드 명령 · 게시 순서)
- **재현·검증**: `npm run play:upload -- --verify --track internal` 이 403 이면 아직 권한이 없다. 권한이 붙으면 `npm run play:upload -- --aab D:/builds/tts_sentence/sentencetts-vc1.aab --track internal --status draft`
- 핵심: ① Play 앱은 만들었다(앱 ID `4973195218256924403` · ko-KR) ② vc1 AAB 는 구웠고 업로드 키 서명·릴리스 실행까지 확인했다 ③ 남은 것은 **서비스 계정 권한(사용자) → 업로드 → 테스터 목록 `사장님 검증 전용` 연결 → 게시**
- 주의: 권한은 `앱을 테스트 트랙으로 출시` 하나만. 프로덕션 권한은 끈 채로 확인한다(`common/PLAY_RELEASE_AUTOMATION.md` §4 · §5.13 다이얼로그 스크롤 함정). 그다음 할 일은 실기기에서 한국어로 들어 보기다(아래 백로그 유저 피해 1)

## 1. 백로그
### 유저 피해가 실재
| 순위 | 항목 | 문서 | 왜 급한가 |
|---|---|---|---|
| 1 | 부모님 폰 한국어 TTS 음성 데이터 확인 | `docs/TTS_SYSTEM.md` §2 | 없으면 한국어를 영어 목소리가 읽는다 |
| 2 | 🙋 Play 밖 설치(APK 직접 설치)가 Google 개발자 인증 요건에 걸리는지 | `CLAUDE.md` §15 ❓ | 걸리면 설치 방법이 바뀐다 |

### 보안·수익
| 순위 | 항목 | 문서 | 왜 급한가 |
|---|---|---|---|
| ~~1~~ | ~~서명한 릴리스 APK~~ → ✅ 2026-09-26 업로드 키 · 서명 플러그인 · `npm run build:aab` 게이트 | `docs/BUILD.md` §1·§2 | 끝났다 |
| 1 | 🙋 업로드 키 백업 방식 확인(지금은 `C:\project\secrets` 한 곳뿐) | `docs/BUILD.md` §2 | 잃으면 되돌릴 수 없다 |

### 게임성·검증 도구
| 순위 | 항목 | 문서 | 왜 급한가 |
|---|---|---|---|
| 1 | 분 타이머 · 셔플 · 빠르기 · 프리셋 두 개 바꿔 듣기 · 문장 삭제 CASCADE 를 에뮬레이터로 확인 | `docs/PLAYER_SYSTEM.md` §0 · `docs/PLAN.md` Phase 2 | 코드만 있고 화면 확인이 없다 |

### 🔴 지금 막힌 것
| 순위 | 항목 | 문서 | 왜 급한가 |
|---|---|---|---|
| 1 | 🙋 서비스 계정에 읽어줘 앱 권한 부여(테스트 트랙 출시만) → `npm run play:upload` → 테스터 연결 → 게시 | `docs/BUILD.md` §4 | 사용자 지시(내부 테스트 등록)가 여기서 멈춰 있다. 권한 부여라 세션이 못 한다 |
| 2 | vc2: 안 쓰는 권한 4개(`DUMP`·저장소 2개·`VIBRATE`)를 `blockedPermissions` 로 | `docs/BUILD.md` §3 | 처리방침·데이터 보안 선언을 좁게 쓸 수 있다 |
| 3 | 비공개·프로덕션으로 갈 때 FGS(미디어 재생) 시연 영상 | `docs/BUILD.md` §3 | 내부 테스트는 검토가 없어 안 걸린다 |

### 표면 흠 (한두 줄 수정)
| 순위 | 항목 | 문서 | 왜 급한가 |
|---|---|---|---|
| 1 | 🙋 화면 문구 사람 검수 | `locales/ko.json` · `common/KOREAN_WRITING.md` §2 | 사용자 노출 문구는 검수 대상이다 |
| 2 | 앱 이름·아이콘 확정(지금은 임시 `읽어줘` · PIL 아이콘) | `CLAUDE.md` §14 미결정 A | 출시를 정할 때 |

### 문서가 코드보다 낡은 것 (되돌리기 사고 방지)
없음 (2026-09-26 커밋에서 문서와 코드를 같이 맞췄다)

## 2. 오늘 끝낸 것
- 설계 정본 · docs 7종 · 첫 구현(문장·재생목록·프리셋·백그라운드 반복 재생) · 스킬 8종 · 훅 2개: `153e16f` (검증: 에뮬레이터 화면 꺼짐 75초 연속 재생 · N회/무한 반복 · 미디어 키 [다음] · 재생목록 가운데부터 · "이번 바퀴 끝나면")
- Play Console 앱 생성 · 업로드 키 · 서명 플러그인 · 빌드/업로드 스크립트 · vc1 AAB · 릴리스 실행 확인: (이 인계와 같은 커밋) (검증: SHA1 대조 · 에뮬레이터 릴리스 APK 로 [듣기] → PLAYING)
- 수정사항 시트 `tts_sentence` 탭 생성(`양식` 복사) · `/check` 기준선 0건
- `common/DEV_ALLOCATION.md` §1·§3 에 행 추가(백업 `.bak-2609260315`) · AVD `tts_sentence` 생성 · `D:\builds\tts_sentence\` 생성

## 3. 대기 중
- 🔴 서비스 계정에 읽어줘 앱 권한 부여 (사용자 · 자동 모드 분류기가 세션의 권한 부여를 막았다)
- 화면 문구 검수 (사용자)
- 앱 이름·아이콘 · 출시 여부 (사용자 · 부모님 반응을 본 뒤)
