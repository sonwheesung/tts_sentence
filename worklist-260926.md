# 내일(2026-09-26) 작업 목록 · 2026-09-25 세션 인계

## 0. 바로 시작할 것: 실기기에서 한국어로 들어 보기 (사용자 확인 필요)
- **설계 정본**: `docs/TTS_SYSTEM.md` §2 (한국어 목소리 없는 기기) · `docs/PLAYER_SYSTEM.md` §0 (⏸ 항목)
- **재현·검증**: `ANDROID_SERIAL=<폰 serial> npx expo run:android --port 8093` 로 실기기에 개발 빌드를 깐다. 한국어 문장 하나를 `목소리 관리` 에서 한국어 목소리로 골라 듣는다. 화면을 끄고 잠금화면에서 [이전]/[다음] 을 누른다.
- 핵심: ① 에뮬레이터에는 한국어 목소리가 없어서 한국어 발음을 아직 한 번도 못 들었다 ② 잠금화면 [이전] 은 확인 안 했다 ③ 여기가 통과해야 부모님 폰에 설치할 APK 를 만든다
- 주의: 실기기가 붙어 있으면 bare `adb` 가 폰으로 간다. 모든 adb 에 `-s` 를 붙인다(`common/EMULATOR_POOL.md` §2).

## 1. 백로그
### 유저 피해가 실재
| 순위 | 항목 | 문서 | 왜 급한가 |
|---|---|---|---|
| 1 | 부모님 폰 한국어 TTS 음성 데이터 확인 | `docs/TTS_SYSTEM.md` §2 | 없으면 한국어를 영어 목소리가 읽는다 |
| 2 | 🙋 Play 밖 설치(APK 직접 설치)가 Google 개발자 인증 요건에 걸리는지 | `CLAUDE.md` §15 ❓ | 걸리면 설치 방법이 바뀐다 |

### 보안·수익
| 순위 | 항목 | 문서 | 왜 급한가 |
|---|---|---|---|
| 1 | 서명한 릴리스 APK(업로드 키는 config plugin 으로) | `common/PLAY_FIRST_UPLOAD.md` §3 · `common/BUILD_ARTIFACTS.md` §1 | 지금 릴리스 빌드는 공개 디버그 키로 서명된다 |

### 게임성·검증 도구
| 순위 | 항목 | 문서 | 왜 급한가 |
|---|---|---|---|
| 1 | 분 타이머 · 셔플 · 빠르기 · 프리셋 두 개 바꿔 듣기 · 문장 삭제 CASCADE 를 에뮬레이터로 확인 | `docs/PLAYER_SYSTEM.md` §0 · `docs/PLAN.md` Phase 2 | 코드만 있고 화면 확인이 없다 |

### 표면 흠 (한두 줄 수정)
| 순위 | 항목 | 문서 | 왜 급한가 |
|---|---|---|---|
| 1 | 🙋 화면 문구 사람 검수 | `locales/ko.json` · `common/KOREAN_WRITING.md` §2 | 사용자 노출 문구는 검수 대상이다 |
| 2 | 앱 이름·아이콘 확정(지금은 임시 `읽어줘` · PIL 아이콘) | `CLAUDE.md` §14 미결정 A | 출시를 정할 때 |

### 문서가 코드보다 낡은 것 (되돌리기 사고 방지)
없음 (2026-09-26 커밋에서 문서와 코드를 같이 맞췄다)

## 2. 오늘 끝낸 것
- 설계 정본 · docs 7종 · 첫 구현(문장·재생목록·프리셋·백그라운드 반복 재생) · 스킬 8종 · 훅 2개: `153e16f` (검증: 에뮬레이터 화면 꺼짐 75초 연속 재생 · N회/무한 반복 · 미디어 키 [다음] · 재생목록 가운데부터 · "이번 바퀴 끝나면")
- 수정사항 시트 `tts_sentence` 탭 생성(`양식` 복사) · `/check` 기준선 0건
- `common/DEV_ALLOCATION.md` §1·§3 에 행 추가(백업 `.bak-2609260315`) · AVD `tts_sentence` 생성 · `D:\builds\tts_sentence\` 생성

## 3. 대기 중
- 화면 문구 검수 (사용자)
- 앱 이름·아이콘 · 출시 여부 (사용자 · 부모님 반응을 본 뒤)
