# 읽어줘 (tts_sentence) · 설계 정본

> 이 문서는 Claude Code 가 개발을 진행하기 위한 **단일 기준 문서**다.
> 모호하면 **§2 핵심 기둥**을 우선한다. **새 결정은 코드보다 먼저 이 문서에 반영한 뒤** 진행한다.
>
> 색인·구현 현황은 [`docs/README.md`](./docs/README.md), 착수 순서는 [`docs/PLAN.md`](./docs/PLAN.md),
> 문서 규율은 [`docs/DOC_DISCIPLINE.md`](./docs/DOC_DISCIPLINE.md).
> 사업자·연락처·스토어 정보의 단일 출처는 `C:\project\common\BUSINESS_INFO.md` (**커밋 금지 파일**).
> 포트·에뮬레이터 할당의 단일 출처는 `C:\project\common\DEV_ALLOCATION.md`.
> 커밋 규약은 `C:\project\common\COMMIT_CONVENTION.md`, 문서 체계는 `C:\project\common\DOC_SYSTEM.md`.
> - **한국어 문장 규칙**: `C:\project\common\KOREAN_WRITING.md` (새로 쓰는 글에 적용한다)
> - **세션 협업 규칙**: `C:\project\common\SESSION_PROTOCOL.md` (형제 앱과 공유하는 정본)
> - **화면 깊이와 뒤로가기**: `C:\project\common\UI_NAVIGATION.md`
> - **에뮬레이터**: `C:\project\common\EMULATOR_POOL.md` (이 프로젝트 값은 `DEV_ALLOCATION.md` §1·§3)
> - **비밀값 다루기**: `C:\project\common\SECRET_HANDLING.md` (🔴 이 레포는 공개 저장소다. 푸시 전에 스테이징을 본다)
> - **하루 닫기·다시 열기**: `C:\project\common\WORKLIST_HANDOVER.md` (루트 `worklist-YYMMDD.md`)
> - **수정사항 시트**: `C:\project\common\FIX_REQUESTS.md` (탭 `tts_sentence` · `/check` 스킬)
> - **빌드 산출물 보관**: `C:\project\common\BUILD_ARTIFACTS.md` (`D:\builds\tts_sentence\`)
> - **스킬 색인**: [`.claude/skills/README.md`](./.claude/skills/README.md). 검증 항목은 `docs/README.md` §3 이 정본이다(스킬에 베끼지 않는다)
>
> 나중에 걸리는 공용 문서(그때 읽는다): 광고를 붙일 때 `ADMOB_OPERATIONS.md` · `APP_ADS_TXT.md` ·
> Play 에 처음 올릴 때 `PLAY_FIRST_UPLOAD.md` · `R8_OBFUSCATION.md` · `CLOSED_TESTING.md` · `PLAY_RELEASE_AUTOMATION.md` · `PLAY_CONSOLE_STATUS.md` ·
> 프로덕션 전 `PRE_LAUNCH_CHECK.md` · `GLOBAL_DATA_COMPLIANCE.md` §6 · `STORE_SCREENSHOTS.md` · 오픈소스 고지는 `GAME_ASSET_SOURCING.md` §3.3 ·
> OTA 를 붙일 때 `OTA_RULES.md` · 아이콘을 새로 만들 때 `ASSET_GENERATION.md`.
> 해당 없음: `ACCOUNT_LIFECYCLE` · `SOCIAL_LOGIN`(로그인 없음) · `LANDSCAPE_APP`(세로) · `COACH_MARKS` · `MODEL_3D_PIPELINE` · `UNITY_DEV`.

작성일: 2026-09-25 · 사용자와의 대화(질문 두 차례)에서 확정한 요구사항을 옮긴 것. 별도 기획서는 없다.

---

## 1. 한 줄 요약

> **내가 고른 문장을, 음악처럼 틀어 두고 듣는 앱.**

사용자가 문장을 저장하고, 기기에 들어 있는 TTS 로 읽어 듣는다. 재생목록·반복·백그라운드 재생 등
**음악 앱의 문법**을 그대로 쓰되, 곡 대신 문장을 재생한다.

- 대상: **사용자의 부모님**(시니어). 반응이 좋으면 출시하고, 아니면 앱을 삭제한다(2026-09-25 사용자).
- 플랫폼: **Android 만**
- 출시 범위: **한국만**. 화면 언어는 **한국어 하나**
- 수익: 광고 **자리만** 잡아 둔다. AdMob 연동은 하지 않는다(§7)

---

## 2. 핵심 기둥 (설계 충돌 시 판단 기준)

1. **문장은 글자만 있으면 끝난다.** 문장 저장에 필요한 입력은 본문 하나다. 음성(TTS 프리셋)은 선택이고,
   고르지 않으면 기본 프리셋으로 읽는다. 메모·번역·태그는 **만들지 않는다**(2026-09-25 사용자 · 결정 #6).

2. **화면을 꺼도 끊기지 않는다.** 백그라운드 재생은 부가 기능이 아니라 **가용성 요구사항**이다.
   그래서 재생 대기열·반복·간격·슬립 타이머는 **JS 가 아니라 네이티브 서비스**가 쥔다(결정 #9).
   JS 타이머로 다음 문장을 넘기자는 제안은 이 기둥으로 기각한다.

3. **무료이고 기기 안에서 끝난다.** 서버·로그인·네트워크가 없다. 음성은 **기기에 설치된 TTS 엔진**만 쓴다.
   만든 음성 파일은 앱 캐시에서만 재생하고, **내보내기·공유 기능을 만들지 않는다**(라이선스 · 결정 #4).

4. **부모님이 쓴다.** 글자와 누르는 영역을 크게 잡고, 한 화면에 한 가지 일만 둔다.
   설정은 기본값만으로 바로 쓸 수 있어야 한다. 기능을 늘리는 제안보다 **헷갈리지 않는 쪽**이 이긴다.

5. **음악 앱의 문법을 따른다.** 라이브러리(전체 문장) · 재생목록 · 하단 미니 플레이어 · 재생 화면 ·
   잠금화면/알림 컨트롤. 새 개념을 만들기 전에 음악 앱에서 같은 것을 뭐라고 부르는지 먼저 본다.

---

## 3. MVP 범위

### 반드시 구현 (v1.0)

| 영역 | 내용 |
|---|---|
| 문장 | 추가 · 목록 · 수정 · 삭제 · 문장별 음성 프리셋 지정(선택) |
| 음성 프리셋 | 엔진 + 목소리 + 속도 + 높낮이 묶음 · 추가/수정/삭제 · **기본 프리셋 지정·변경** · 미리 듣기 |
| 재생목록 | 추가 · 이름 변경 · 삭제 · 문장 넣기/빼기 · 순서 바꾸기 · 한 문장이 여러 재생목록에 들어갈 수 있다 |
| 재생 단위 | 재생목록 전체 · 전체 문장 · 문장 하나 (셋 다 반복 적용) |
| 반복 | 대기열 전체를 **N회 또는 무한**(결정 #7) |
| 재생 옵션 | 이전/다음 · 셔플 · 문장 사이 간격 · 재생 속도 · 슬립 타이머 · 재생 중 문장 강조 |
| 백그라운드 | 화면 꺼짐·다른 앱 사용 중에도 재생 · 알림/잠금화면 컨트롤(재생/일시정지/이전/다음) |
| 광고 | 하단 배너 **자리만** 표시(회색 빈 영역 · 결정 #5) |

### 제외 (v1.0)

- 🚫 로그인 · 백업 · 동기화 · 서버 (결정 #8)
- 🚫 클라우드 TTS(유료) · 앱에 음성 모델 넣기 (결정 #3)
- 🚫 음성 파일 내보내기·공유 (결정 #4)
- 🚫 메모 · 번역 · 태그 (결정 #6)
- 🚫 문장 단위 반복(A A A B B B) (결정 #7)
- 🚫 iOS · 다국어
- 🚫 AdMob 실연동 · 광고 제거 결제 (결정 #5)

### 이후 (반응이 좋을 때)

AdMob 연결 → 스토어 출시 준비(처리방침 · 등록정보 · 오픈소스 라이선스 고지) → 백업(파일 내보내기).

---

## 4. 계정 정책

**없음.** 로그인하지 않는다. 데이터는 기기 SQLite 에만 있다. 앱을 지우면 데이터도 사라진다(결정 #8).

---

## 5. 도메인 규칙

- 🔴 **삭제는 참조를 따라 정리한다**(2026-09-25 사용자).
  - 문장을 지우면 그 문장이 들어 있던 **모든 재생목록에서 빠진다**(FK `ON DELETE CASCADE`).
  - 재생목록을 지워도 문장은 남는다.
  - 음성 프리셋을 지우면 그 프리셋을 쓰던 문장은 **기본 프리셋으로 돌아간다**(`ON DELETE SET NULL`).
  - **기본 프리셋은 지울 수 없다.** 먼저 다른 프리셋을 기본으로 지정해야 한다. 프리셋은 항상 1개 이상이다.
- 같은 문장을 한 재생목록에 **여러 번 넣을 수 있다**(음악 앱과 같다). 그래서 재생목록 항목은 자기 id 를 가진다.
- 문장 음성이 비어 있으면 **재생 시점의 기본 프리셋**으로 읽는다. 저장 시점의 기본값을 복사해 두지 않는다.
  기본값을 바꾸면 지정 안 한 문장 전부의 목소리가 같이 바뀌어야 하기 때문이다.
- 문장 길이 상한은 **1,000자**다. Android `TextToSpeech.getMaxSpeechInputLength()` 는 4,000자인데,
  긴 문장은 합성 대기가 길어져 "재생을 눌렀는데 소리가 안 난다"로 읽힌다. (placeholder 2026-09-25)
- 반복 횟수는 **1~99 또는 무한**이다. 기본값은 1회. (placeholder 2026-09-25)
- 재생 중에 재생목록이나 문장을 고쳐도 **지금 도는 대기열은 바뀌지 않는다.** 다음에 재생을 누를 때 반영된다.
  음악 앱에서 재생 중인 대기열이 편집과 따로 노는 것과 같다.

상세는 [`docs/PLAYER_SYSTEM.md`](./docs/PLAYER_SYSTEM.md) · [`docs/TTS_SYSTEM.md`](./docs/TTS_SYSTEM.md) ·
[`docs/DATABASE.md`](./docs/DATABASE.md).

---

## 6. 서버 경계

**서버가 없다.** 모든 데이터와 처리는 기기 안에 있다. 네트워크 권한도 요구하지 않는 것이 목표다.
(⚠ React Native·Expo 가 기본으로 넣는 `INTERNET` 권한은 개발 빌드의 Metro 연결에 필요하다.
릴리스에서 뺄지는 출시를 정할 때 본다. §14 미결정 B)

---

## 7. 수익화 정책

- v1.0 은 **광고 자리만** 둔다. 하단에 배너 크기(320×50)의 회색 빈 영역을 그리고 SDK 는 넣지 않는다(결정 #5).
- 🔴 재생 중 소리를 끊는 **전면 광고는 넣지 않는다**(기둥 2). 배너만 쓴다.
- 부모님 반응이 좋으면 AdMob 을 붙인다. 그때 `C:\project\common\ADMOB_OPERATIONS.md` 를 따르고
  처리방침(광고 식별자)을 함께 만든다.

---

## 8. 음성(TTS) 방향

- 기기 내장 TTS 엔진(Android `TextToSpeech`)만 쓴다. Google 음성 엔진·삼성 TTS 등 **기기에 깔린 엔진 전부**가 후보다.
- "여러 TTS" 는 **음성 프리셋**이다: 엔진 + 목소리 + 속도 + 높낮이를 묶어 이름을 붙인 것(결정 #3).
- 기기마다 설치된 목소리가 다르다. 프리셋이 가리키는 목소리가 이 기기에 없으면 **그 엔진의 기본 목소리**로 읽고,
  화면에 "이 기기에 없는 목소리"라고 표시한다.
- 라이선스 판단과 근거는 [`docs/TTS_SYSTEM.md`](./docs/TTS_SYSTEM.md) §5.

---

## 9. 재생 방향

- 재생은 **파일로 합성한 뒤 재생**한다. `speak()` 로 바로 읽지 않는다(결정 #9).
  - 문장 → `synthesizeToFile` 로 WAV 생성(캐시) → 끝에 간격만큼 무음을 붙인다 → Media3 ExoPlayer 가 재생
  - 재생은 `MediaSessionService` 포그라운드 서비스가 한다. 알림·잠금화면 컨트롤이 여기서 나온다.
- 대기열·반복 횟수·슬립 타이머는 네이티브 서비스가 센다. JS 는 대기열을 **넘겨주고 상태를 받아 그린다.**
- 상세는 [`docs/PLAYER_SYSTEM.md`](./docs/PLAYER_SYSTEM.md).

---

## 10. 화면 방향

- 뿌리 탭 3개: **문장** · **재생목록** · **설정**(음성 프리셋 포함). 탭 위에 **미니 플레이어**가 떠 있다.
- 글자 기준 크기를 형제 앱보다 크게 잡는다(본문 18 · 문장 카드 20). 누르는 영역 최소 56dp.
- 뒤로가기 아이콘은 **좌측 상단**(플랫폼 관례 · `UI_NAVIGATION.md` §2).
- 상세는 [`docs/UI_GUIDE.md`](./docs/UI_GUIDE.md).

---

## 11. 기술 스택

**기준 프로젝트는 `C:\project\link_memo`(LinkMemo)** 다. 버전 조합·구조·문서 방식·커밋 규칙을 승계한다.
오디오 백그라운드 설정은 `C:\project\snoreLess`(SnoreLess)를 참고했다.

| 영역 | 선택 | 버전 |
|---|---|---|
| 프레임워크 | Expo (CNG · 개발 빌드) | `~54.0.35` |
| 런타임 | React Native · React | `0.81.5` · `19.1.0` |
| 라우팅 | expo-router | `~6.0.24` |
| 저장 | expo-sqlite | `~16.0.10` |
| 상태 | zustand | `^5` |
| 문구 | i18next · react-i18next (ko 하나) | `^26` · `^17` |
| 파일 | expo-file-system · expo-crypto(캐시 키) | SDK 54 |
| TTS · 재생 | **로컬 Expo 모듈 `modules/sentence-audio`**(Kotlin) · Android `TextToSpeech` · `androidx.media3` | Media3 `1.8.0`(expo-audio 1.1.1 과 같은 값) |
| 언어 | TypeScript strict · `any` 금지(ESLint error) | `~5.9.2` |

- 🔴 **Expo Go 로는 돌지 않는다.** 로컬 네이티브 모듈이 있어서 `expo run:android` 개발 빌드가 필요하다.
- 포트: Metro **8093**, 에뮬레이터 **`tts_sentence` · 5586** (`DEV_ALLOCATION.md` 가 정본).

---

## 12. 프로젝트 구조

```
app/                    expo-router 화면
  (tabs)/               뿌리 탭: index(문장) · playlists · settings
  sentence/             문장 추가·수정
  playlist/             재생목록 상세·문장 고르기
  preset/               음성 프리셋 편집
  player.tsx            재생 화면
components/             공통 부품(Screen · Header · MiniPlayer · AdSlot …)
features/               도메인 로직(sentences · playlists · presets · player)
db/                     SQLite 연결 · 마이그레이션
lib/                    i18n · 유틸
locales/ko.json         화면 문구
modules/sentence-audio/ 로컬 Expo 모듈(TTS 합성 + 재생 서비스, Kotlin)
theme/                  토큰
docs/                   세부 문서
```

---

## 13. 표준 작업 순서

① 플랜 → ② **문서(코드보다 먼저)** → ③ 개발 → ④ 검증(`docs/README.md` §3) → ⑤ 커밋(문서 포함)

---

## 14. 결정 로그 · 미결정

| # | 날짜 | 결정 | 근거 · 포기한 것 |
|---|---|---|---|
| #1 | 2026-09-25 | **Android 만 · 한국만 · 한국어만** | 부모님용으로 먼저 만들고 반응을 본다. 포기: iOS·다국어 |
| #2 | 2026-09-25 | 스택은 형제 앱(Expo 54)을 따른다 | 사용자 지시. LinkMemo 승계 |
| #3 | 2026-09-25 | **TTS 는 기기 내장 엔진만(무료)**, "여러 TTS" 는 음성 프리셋(엔진+목소리+속도+높낮이) | 무료 · 광고 앱에서 라이선스 부담이 가장 작다. 포기: 기기와 무관한 일정한 목소리(클라우드·내장 모델) |
| #4 | 2026-09-25 | 합성한 음성 파일을 **내보내거나 공유하지 않는다** | 엔진 음성의 재배포로 읽힐 수 있다. 앱 캐시 안 재생만 한다 |
| #5 | 2026-09-25 | 광고는 **자리만** 표시, AdMob 연동 안 함 · 전면 광고 금지 | 사용자 지시. 반응이 좋으면 붙인다 |
| #6 | 2026-09-25 | 문장은 **본문 하나**(+ 선택 음성). 메모·번역·태그 없음 | 사용자 지시 |
| #7 | 2026-09-25 | 반복은 **대기열 전체 N회 또는 무한**(A안). 재생 단위는 재생목록 · 전체 문장 · 문장 하나, 모두 반복 적용 | 사용자 지시. 포기: 문장 단위 반복 |
| #8 | 2026-09-25 | 로그인·백업 없이 **기기에만 저장** | 사용자 지시 |
| #9 | 2026-09-25 | **파일 합성 + Media3 네이티브 재생 서비스**. 대기열·반복·타이머는 네이티브가 센다 | 백그라운드에서 JS 가 다음 문장을 넘기는 구조는 화면이 꺼지면 끊길 수 있다(기둥 2). expo-audio 의 잠금화면 컨트롤에는 이전/다음이 없다(1.1.1 소스 확인: `COMMAND_SEEK_TO_NEXT` 를 제거한다). 대가: Kotlin 모듈을 직접 유지한다 |
| #10 | 2026-09-25 | 패키지 `com.vivacegames.sentencetts` · 앱 이름 **읽어줘** | 사용자: "COM.VIVACE" + 나머지는 추천대로. 형제 앱 접두사가 `com.vivacegames` 다(`BUSINESS_INFO.md` 실측). ⚠ Play 에 처음 올리기 전까지는 바꿀 수 있다 |
| #11 | 2026-09-25 | 재생 옵션 전부 넣는다: 이전/다음 · 셔플 · 문장 간격 · 재생 속도 · 슬립 타이머 · 재생 중 강조 | 사용자 지시("다 넣어줘") |
| #12 | 2026-09-25 | 문장을 지우면 모든 재생목록에서 빠진다 · 한 재생목록에 같은 문장 중복 허용 | 사용자 지시(삭제) · 중복은 음악 앱 관례 |
| #13 | 2026-09-26 | **커밋·푸시는 세션 판단으로 한다. 원격은 공개 저장소다** | 사용자 지시. 그래서 푸시 전마다 스테이징에 비밀값·키스토어·`BUSINESS_INFO` 내용·개인정보가 없는지 본다(`common/SECRET_HANDLING.md`) |
| #14 | 2026-09-26 | `/check` 스킬 폴더는 **git 에 올리지 않는다** | 형제 앱은 `*.json` 만 뺀다. 이 레포는 공개라 `SKILL.md` 의 비공개 시트 주소도 올리지 않는다. 원본이 `common/FIX_REQUESTS.md` §7 이라 잃어도 다시 복사하면 된다 |

### ⚠ 미결정

- **A. 앱 이름·아이콘 확정.** 지금은 임시(`읽어줘`). 출시를 정할 때 확정한다.
- **B. 릴리스 빌드에서 `INTERNET` 권한을 뺄지.** 광고를 붙이면 필요하므로 광고 결정과 같이 본다.

---

## 15. 선행 의존

| 항목 | 상태 |
|---|---|
| 에뮬레이터 AVD `tts_sentence`(5586) | ✅ 2026-09-26 생성 · `D:\emulators	ts_sentence` (`DEV_ALLOCATION.md` §3) |
| Play Console 앱 | ➖ 출시를 정할 때 |
| AdMob 앱 · 광고 단위 | ➖ 반응이 좋을 때(결정 #5) |
| 수정사항 시트 탭 `tts_sentence` | ✅ 2026-09-26 `양식` 복사로 생성 · `/check` 기준선 0건 저장 (`common/FIX_REQUESTS.md` §2) |
| 빌드 보관 폴더 `D:uilds	ts_sentence\` | ✅ 2026-09-26 생성 (`common/BUILD_ARTIFACTS.md` §1) |
| ❓ Android 개발자 인증(Play 밖 설치) | 부모님 폰에 APK 를 직접 설치하는 방식이 Google 개발자 인증 요건에 걸리는지 **확인 안 됨**. `BUSINESS_INFO.md` §4 는 형제 앱에 "해당 없음"으로 적었는데, 이 앱은 바로 Play 밖 설치다. 설치 전에 본다 |

---

## 16. 현재 상태 (2026-09-26)

- Phase 0·1·3 완료, 2·4 진행 중. 진행 현황은 [`docs/PLAN.md`](./docs/PLAN.md) 와 `docs/README.md` §2 가 정본이다.
- 에뮬레이터에서 확인한 것: 합성 · 화면 꺼진 상태 반복 재생(N회·무한) · 알림 컨트롤 · 재생목록 가운데부터 재생 · "이번 바퀴 끝나면".
- 🔴 착수 전 할 일
  - 화면 문구 사람 검수(`KOREAN_WRITING.md` §2). 문안은 `locales/ko.json` 한 파일이다.
  - 부모님 폰의 한국어 TTS 음성 데이터 확인(TTS_SYSTEM.md §2).
- 다음: 실기기 확인(한국어 목소리 · 잠금화면 이전/다음) → 서명한 릴리스 APK → 부모님 설치.
